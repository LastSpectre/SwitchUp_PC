public class Adventurer : Character
{
    private void Awake()
    {
        // sets default values
        m_CharacterName = "Adventurer";
        m_Type = ECharacterType.NEUTRAL;
    }

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
