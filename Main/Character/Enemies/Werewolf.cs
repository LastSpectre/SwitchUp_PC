public class Werewolf : Character
{
    private void Awake()
    {
        // sets default values
        m_CharacterName = "Werewolf";
        m_Type = ECharacterType.DARK;
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
