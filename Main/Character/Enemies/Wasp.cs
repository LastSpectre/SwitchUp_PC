public class Wasp : Character
{
    private void Awake()
    {
        // sets default values
        m_CharacterName = "Wasp";
        m_Type = ECharacterType.NATURE;
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
