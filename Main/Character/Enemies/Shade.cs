public class Shade : Character
{
    private void Awake()
    {
        // sets default values
        m_CharacterName = "Shade";
        m_Type = ECharacterType.CHAOS;
    }

    // Start is called before the first frame update
    void Start()
    {
         
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
