public class Mandrake : Character
{
    private void Awake()
    {
        // sets default values
        m_CharacterName = "Mandrake";
        m_Type = ECharacterType.NATURE;
    }

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
