public class Satyr : Character
{
    private void Awake()
    {
        // sets default values
        m_CharacterName = "Satyr";
        m_Type = ECharacterType.ANCIENTS;
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
