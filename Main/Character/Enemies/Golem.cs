public class Golem : Character
{
    private void Awake()
    {
        // sets default values
        m_CharacterName = "Golem";
        m_Type = ECharacterType.ANCIENTS;
    }

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
